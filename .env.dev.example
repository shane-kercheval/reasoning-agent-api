# Development Environment Configuration
# Copy to .env and fill in your secrets
# All other settings use sensible defaults from api/config.py and docker-compose.yml

# =============================================================================
# REQUIRED SECRETS
# =============================================================================

# OpenAI API Key (required - used by LiteLLM proxy)
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic API Key (optional - if using Claude models)
# ANTHROPIC_API_KEY=your-anthropic-key-here

# Brave Search API Key (optional - if using web search tools)
# BRAVE_SEARCH_API=your-brave-search-api-key-here

# =============================================================================
# LITELLM CONFIGURATION
# =============================================================================

# Master Key for LiteLLM admin operations
# Generate: python -c "import secrets; print('sk-' + secrets.token_urlsafe(32))"
LITELLM_MASTER_KEY=

# Virtual Keys (generated by `make litellm_setup` - LEAVE EMPTY initially)
LITELLM_API_KEY=
LITELLM_TEST_KEY=
LITELLM_EVAL_KEY=

# Base URL (environment-specific)
# Local dev: http://localhost:4000
# Docker: http://litellm:4000 (set in docker-compose.yml)
# LITELLM_BASE_URL=http://localhost:4000

# =============================================================================
# DATABASE PASSWORDS
# =============================================================================

# Generate secure passwords: python -c "import secrets; print(secrets.token_urlsafe(16))"
LITELLM_POSTGRES_PASSWORD=
PHOENIX_POSTGRES_PASSWORD=
REASONING_POSTGRES_PASSWORD=

# Reasoning Database URL (environment-specific)
# Local dev (outside Docker): Uncomment and use localhost:5434
# Docker: Uses default from docker-compose.yml (postgres-reasoning:5432)
# REASONING_DATABASE_URL=postgresql+asyncpg://reasoning_user:${REASONING_POSTGRES_PASSWORD}@localhost:5434/reasoning

# =============================================================================
# DEVELOPMENT OVERRIDES
# =============================================================================

# Disable auth for easier local testing (default: true in production)
REQUIRE_AUTH=false

# Enable debug mode for verbose logging (default: false)
DEBUG=true

# =============================================================================
# SERVICE URLS (Docker Internal - Used by containers)
# =============================================================================

# Tools API endpoint for reasoning-api (Docker internal)
TOOLS_API_URL=http://tools-api:8001

# LiteLLM proxy endpoint (Docker internal)
LITELLM_BASE_URL=http://litellm:4000

# Phoenix OTLP collector endpoint (Docker internal)
PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:4317

# Phoenix project name for trace organization
PHOENIX_PROJECT_NAME=reasoning-agent

# Enable OpenTelemetry tracing
ENABLE_TRACING=true

# =============================================================================
# CLIENT CONFIGURATION (External URLs - Used by Electron/Browser)
# =============================================================================

# Reasoning API URL (external - for client access)
REASONING_API_URL=http://localhost:8000

# Tools API URL (external - for client direct access)
TOOLS_API_URL_CLIENT=http://localhost:8001

# Reasoning API token (optional - leave empty if REQUIRE_AUTH=false)
REASONING_API_TOKEN=

# =============================================================================
# TOOLS API CONFIGURATION
# =============================================================================

# Volume Mounts
# Configure in docker-compose.override.yml (see docker-compose.override.yml.example)
# Pattern:
#   - Read-write: /local/path:/mnt/read_write/name:rw
#   - Read-only:  /local/path:/mnt/read_only/name:ro

# API Keys for Tools
GITHUB_TOKEN=  # Optional: GitHub personal access token for PR/git tools
BRAVE_API_KEY=${BRAVE_SEARCH_API}  # Uses same key as Brave Search above

# =============================================================================
# QUICK START
# =============================================================================
# 1. Copy this file: cp .env.dev.example .env
# 2. Add your OPENAI_API_KEY above
# 3. Generate passwords and run: make litellm_setup
# 4. Copy generated LITELLM_API_KEY, LITELLM_TEST_KEY, LITELLM_EVAL_KEY above
# 5. Run: make api (for local) or make docker_up (for Docker)
