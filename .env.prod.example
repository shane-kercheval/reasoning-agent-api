# Production Environment Configuration
# All other settings use sensible defaults from api/config.py and docker-compose.yml

# =============================================================================
# REQUIRED SECRETS
# =============================================================================

# OpenAI API Key (required - used by LiteLLM proxy)
OPENAI_API_KEY=

# Anthropic API Key (optional - if using Claude models)
# ANTHROPIC_API_KEY=

# =============================================================================
# LITELLM CONFIGURATION
# =============================================================================

# Master Key for LiteLLM admin operations
# Generate: python -c "import secrets; print('sk-' + secrets.token_urlsafe(32))"
# SECURITY: Store in secrets manager (AWS Secrets Manager, etc.)
LITELLM_MASTER_KEY=

# Virtual Keys (generated by setup script)
# SECURITY: Rotate regularly, store in secrets manager
LITELLM_API_KEY=

# =============================================================================
# DATABASE PASSWORDS
# =============================================================================

# Generate strong passwords: python -c "import secrets; print(secrets.token_urlsafe(32))"
# SECURITY: Store in secrets manager, rotate regularly
LITELLM_POSTGRES_PASSWORD=
PHOENIX_POSTGRES_PASSWORD=
REASONING_POSTGRES_PASSWORD=

# Reasoning Database URL
# Docker: Uses default from docker-compose.yml (postgres-reasoning:5432)
# Production: Set explicitly if using external database
# REASONING_DATABASE_URL=postgresql+asyncpg://reasoning_user:${REASONING_POSTGRES_PASSWORD}@postgres-reasoning:5432/reasoning

# =============================================================================
# AUTHENTICATION (Production - REQUIRED)
# =============================================================================

# API Bearer Tokens (comma-separated)
# SECURITY: Generate strong tokens, rotate regularly
# Example: API_TOKENS=token1,token2,token3
API_TOKENS=

# Enable authentication (default: true)
REQUIRE_AUTH=true

# =============================================================================
# SERVICE URLS (Docker Internal - Used by containers)
# =============================================================================

# Tools API endpoint for reasoning-api (Docker internal)
TOOLS_API_URL=http://tools-api:8001

# LiteLLM proxy endpoint (Docker internal)
LITELLM_BASE_URL=http://litellm:4000

# Phoenix OTLP collector endpoint (Docker internal)
PHOENIX_COLLECTOR_ENDPOINT=http://phoenix:4317

# Phoenix project name for trace organization
PHOENIX_PROJECT_NAME=reasoning-agent

# Enable OpenTelemetry tracing
ENABLE_TRACING=true

# =============================================================================
# CLIENT CONFIGURATION (External URLs - Adjust for production)
# =============================================================================

# Reasoning API URL (external - for client access)
# Production: Update to your production domain
REASONING_API_URL=https://your-domain.com

# Tools API URL (external - for client direct access)
# Production: If exposing tools-API externally, update URL
TOOLS_API_URL_CLIENT=https://tools.your-domain.com

# Reasoning API token (set to valid production token)
REASONING_API_TOKEN=

# =============================================================================
# TOOLS API CONFIGURATION
# =============================================================================

# Path Configuration (Volume Mounts for Docker)
# Production: Update to match server paths
REPOS_PATH=/opt/repos
DOWNLOADS_PATH=/opt/downloads
PLAYBOOKS_PATH=/opt/playbooks

# API Keys for Tools
# SECURITY: Store in secrets manager
GITHUB_TOKEN=  # Optional: GitHub personal access token
BRAVE_API_KEY=  # Optional: Brave Search API key for web search tool

# =============================================================================
# PRODUCTION CHECKLIST
# =============================================================================
# [ ] All API keys stored in secrets manager
# [ ] Database passwords are strong (32+ characters)
# [ ] API_TOKENS are set with strong values
# [ ] REQUIRE_AUTH=true (authentication enabled)
# [ ] DEBUG=false (debug mode disabled)
# [ ] SSL/TLS certificates configured
# [ ] Firewall rules configured
# [ ] Backup strategy in place
# [ ] Monitoring and alerting configured
