{
  "mcpServers": {
    "local_bridge": {
      "url": "${MCP_BRIDGE_URL:-http://localhost:9000/mcp/}",
      "transport": "http",
      "enabled": false,
      "description": "Local MCP bridge for stdio servers (filesystem, mcp-this, github)",
      "_setup": "See README_MCP_QUICKSTART.md for setup instructions"
    },
    "demo_tools": {
      "url": "${DEMO_MCP_SERVER_URL:-http://localhost:8001/mcp/}",
      "transport": "http",
      "enabled": false,
      "description": "Demo MCP server with fake tools (weather, search, stocks, etc.)",
      "_note": "Start with: make demo_mcp_server (or docker compose --profile demo up)"
    }
  },
  "_instructions": {
    "setup": "Copy this file to mcp_servers.json and enable servers you want to use",
    "command": "cp config/mcp_servers.example.json config/mcp_servers.json",
    "enable": "Set 'enabled': true for servers you want to use"
  }
}
