{
  "mcpServers": {
    "local_bridge": {
      "url": "${MCP_BRIDGE_URL:-http://localhost:9000/mcp/}",
      "transport": "http",
      "enabled": true,
      "description": "Local MCP bridge for stdio servers (test config)"
    }
  },
  "_comment": "Test configuration for API integration with MCP bridge"
}
